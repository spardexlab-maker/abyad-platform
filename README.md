
# 🩺 مشروع أَبْيَض | Abyad

**منصة طبية عراقية شاملة:** تربط المرضى بالأطباء، مراكز التجميل، والمختبرات. مصممة لتعمل كتطبيق ويب (PWA) وتطبيق موبايل (Android/iOS).

![Project Logo](https://i.ibb.co/6gT3jJV/doctor-illustration.png)

---

## 📑 فهرس المحتويات
1.  [نظرة عامة](#-نظرة-عامة)
2.  [المميزات](#-المميزات)
3.  [التقنيات المستخدمة](#-التقنيات-المستخدمة)
4.  [إعداد المشروع وتشغيله](#-إعداد-المشروع-وتشغيله)
5.  [إعداد قاعدة البيانات (Backend)](#-إعداد-قاعدة-البيانات-backend)
6.  [بناء تطبيق الموبايل](#-بناء-تطبيق-الموبايل)

---

## 🌟 نظرة عامة

يهدف "أَبْيَض" لرقمنة القطاع الصحي في العراق عبر توفير 4 بوابات في تطبيق واحد:
*   **للمريض:** حجز مواعيد، سجل طبي إلكتروني، البحث عن الخدمات عبر الخريطة.
*   **للطبيب:** إدارة العيادة، الجدول، وسجلات المرضى.
*   **لمراكز التجميل والمختبرات:** إدارة الحجوزات، العروض، والخدمات.
*   **للإدارة (Admin):** مراقبة النظام والتحكم بالمستخدمين.

---

## 🚀 المميزات

*   **حجز ذكي:** نظام جدولة يمنع تضارب المواعيد.
*   **خرائط تفاعلية:** العثور على أقرب طبيب أو مختبر باستخدام GPS.
*   **دعم كامل للغة العربية:** واجهة مستخدم (UI) مصممة خصيصاً للاتجاه RTL.
*   **الوضع الليلي (Dark Mode):** دعم كامل للثيمات.
*   **سريع جداً:** مبني باستخدام Vite و React 19.

---

## 🛠 التقنيات المستخدمة

*   **Frontend:** React 19, TypeScript, Tailwind CSS.
*   **State Management:** Zustand.
*   **Routing:** Custom Logic (لضمان تجربة Native App سلسة).
*   **Backend:** Supabase (PostgreSQL, Auth, Storage).
*   **Maps:** Leaflet (OpenStreetMap).
*   **Mobile Build:** Capacitor (لتحويل الكود إلى APK/IPA).

---

## 🚀 إعداد المشروع وتشغيله

### 1. المتطلبات
تأكد من تثبيت [Node.js](https://nodejs.org/) الإصدار 18 أو أحدث.

### 2. التثبيت
```bash
# تثبيت المكتبات الأساسية
npm install

# تثبيت مكتبات الموبايل وقاعدة البيانات
npm install @supabase/supabase-js @capacitor/core @capacitor/cli @capacitor/android @capacitor/ios
```

### 3. إعداد البيئة (.env)
قم بإنشاء ملف `.env` في المجلد الرئيسي وأضف مفاتيح Supabase:
```env
VITE_SUPABASE_URL=رابط_مشروعك_في_Supabase
VITE_SUPABASE_ANON_KEY=مفتاح_Anon_الخاص_بك
```

### 4. التشغيل (Web)
```bash
npm run dev
```

---

## 🗄️ إعداد قاعدة البيانات (Backend)

تم فصل توثيق قاعدة البيانات في ملف خاص لسهولة الاستخدام. يحتوي الملف على كود SQL الكامل لإنشاء الجداول وسياسات الأمان.

👉 **[اضغط هنا لفتح ملف إعداد قاعدة البيانات (DATABASE.md)](./DATABASE.md)**

---

## 📱 بناء تطبيق الموبايل

لتحويل المشروع إلى تطبيق أندرويد:

1.  **بناء ملفات الويب:**
    ```bash
    npm run build
    ```

2.  **تهيئة Capacitor (للمرة الأولى فقط):**
    ```bash
    npx cap init Abyad com.abyad.health
    # ملاحظة: تأكد أن webDir هو 'dist'
    ```

3.  **إضافة منصة Android:**
    ```bash
    npx cap add android
    ```

4.  **المزامنة والتشغيل:**
    ```bash
    npx cap sync
    npx cap open android
    ```
    *سيفتح Android Studio، انتظر الـ Gradle Sync ثم اضغط Run.*

---

**تم التطوير بواسطة: Google Gemini (AI)**
